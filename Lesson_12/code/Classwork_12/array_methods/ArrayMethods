Ваш класс ArrayMethods выглядит корректно и содержит необходимые методы для работы с массивами. Вот краткий обзор каждого метода, чтобы убедиться, что всё работает, как задумано:
1. Методы класса ArrayMethods

    fillArray(int a, int b, int n):
        Создаёт массив целых чисел длиной n и заполняет его случайными числами в диапазоне от a до b.
        Примечание: данный метод корректен и выполняет свою задачу.

    printArray(int[] array):
        Выводит элементы массива целых чисел на экран.
        Код также выглядит правильно, и массив будет выводиться в одной строке, разделённый пробелами.

    searchInArray(int[] array, int n):
        Ищет число n в массиве и возвращает true, если оно найдено, или false, если нет.
        Это также реализовано правильно.

    calculatePercent(double[] accounts, double percent):
        Начисляет заданный процент к каждому элементу массива и возвращает новый массив.
        Метод корректен и выполняет требуемую задачу.

    printArrayDouble(double[] array):
        Выводит массив данных типа double на экран.
        Код правильный и будет работать должным образом.

    sumArray(int[] array):
        Суммирует все элементы массива и возвращает сумму.
        Этот метод тоже реализован верно.

    averageValue(double[] array):
        Вычисляет среднее арифметическое для массива double и возвращает это значение.
        Всё правильно реализовано.

Итог

Класс ArrayMethods выполняет все свои функции корректно. Теперь нужно убедиться, что вы вызываете эти методы из класса ArrayMethodsAppl, и что весь код внутри метода main написан правильно.
